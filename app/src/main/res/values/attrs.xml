<resources>

    <!-- Declare custom theme attributes that allow changing which styles are used for button bars depending on the API level.
         ?android:attr/buttonBarStyle is new as of API 11 so this is necessary to support previous API levels. -->
    <declare-styleable name="ButtonBarContainerTheme">
        <attr name="metaButtonBarStyle" format="reference"/>
        <attr name="metaButtonBarButtonStyle" format="reference"/>
    </declare-styleable>

    <declare-styleable name="AppTheme" >
        <attr name="color_bg_news" format="reference|color"/>
        <attr name="color_bg_news_focused" format="reference|color"/>
    </declare-styleable>

    <declare-styleable name="ButtonPreference">
        <attr name="entries"/>
        <attr name="soundeffect" format="reference"/>
        <attr name="colors" format="color"/>
    </declare-styleable>

</resources>
